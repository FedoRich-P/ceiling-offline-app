export function useRooms() {
    // const rooms = useLiveQuery(() => db.rooms.toArray(), []);
    //
    // async function saveRoom(name: string, data: string) {
    //     await db.rooms.put({
    //         name,
    //         data,
    //         updatedAt: Date.now(),
    //         synced: false,
    //     });
    // }
    //
    // async function deleteRoom(id: number) {
    //     await db.rooms.delete(id);
    // }
    //
    // return {
    //     rooms,
    //     saveRoom,
    //     deleteRoom,
    // };
}
